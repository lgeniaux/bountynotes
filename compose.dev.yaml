services:
  web:
    command: npm start
    volumes:
      - ./frontend:/app
      - web_node_modules:/app/node_modules

  api:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app

volumes:
  web_node_modules:
